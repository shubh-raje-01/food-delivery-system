package com.food_delivery_system.restaurant_service.enums;

// Local copy mirrors auth-service's Role enum — no cross-module dependency exists yet.
public enum Role {
    CUSTOMER, RESTAURANT_OWNER, DELIVERY_PARTNER, ADMIN
}
